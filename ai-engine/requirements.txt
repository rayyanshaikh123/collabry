# Core Dependencies (Production-Grade Backend)
# Minimal set of dependencies following clean architecture principles

# === Core Framework ===
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.0
python-multipart==0.0.12

# === LLM & Agent (OpenAI-compatible) ===
openai==1.54.0
langchain==0.3.15
langchain-openai==0.2.11
langchain-core==0.3.15
langchain-community==0.3.15
langgraph==0.2.28

# === Vector Stores ===
# FAISS (default, local, simple)
faiss-cpu==1.8.0

# Chroma (optional, local/cloud)
# chromadb==0.5.0

# Pinecone (optional, cloud production)
# pinecone-client==5.0.0
# langchain-pinecone==0.2.0

# Qdrant (optional, local/cloud)
# qdrant-client==1.11.0

# === Embeddings ===
# OpenAI embeddings (via openai package above)

# Sentence-Transformers (optional, local embeddings)
# sentence-transformers==3.0.0

# HuggingFace (optional, cloud embeddings)
# requests==2.32.0  # for HF API

# === Database ===
pymongo==4.8.0

# === Document Processing ===
pypdf==5.0.0
python-docx==1.1.2
langchain-text-splitters==0.3.0

# === Authentication ===
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# === Environment & Config ===
python-dotenv==1.0.0

# === Utilities ===
requests==2.32.0

# === Development Tools (optional) ===
# pytest==8.3.0
# pytest-asyncio==0.24.0
# black==24.8.0
# ruff==0.6.0

# === Monitoring (optional) ===
# sentry-sdk==2.15.0
